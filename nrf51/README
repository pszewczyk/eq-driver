This is a template for creating nrf5x projects.

BUILDING
Specify SDK root in Makefile, than execute make.
Find Makefile.posix and Makefile.common in sdk directory, you may need to edit them.
Try installing new arm-none-eabi version

ADDING MODULES
To use the nrf modules do the following:
 - Make sure headers locations are specified in makefile
 - Add modules source paths to makefile
 - Define proper ENABLE flag in config/sdk_config.h

FLASHING
Just flash the .hex file with your favourite flashing tool.
